# 复制本文件为 .env 并填写真实值（或直接设置系统环境变量）

# ===== 企业微信：接入模式三选一 =====
# WECOM_MODE=app      # app=自建应用(需要下面 CorpID/AgentId/Secret，管理员配置)
# WECOM_MODE=aibot    # 智能机器人 URL回调(无需管理员)：只需 Token/EncodingAESKey，回复走 response_url
# WECOM_MODE=aibot_ws # 智能机器人 长连接(内网部署)：只需 BotID/Secret，服务端主动连 wss，无需公网地址
WECOM_MODE=app
# 智能机器人回复格式：markdown(推荐) 或 text
# AIBOT_REPLY_TYPE=markdown

# ---- 方案一：自建应用 ----
WECOM_CORP_ID=ww1234567890abcdef
WECOM_AGENT_ID=1000002
WECOM_SECRET=your_app_secret_here
# 接收消息 -> 设置API接收 里填写的 Token 和 EncodingAESKey
WECOM_TOKEN=your_token_here
WECOM_ENCODING_AES_KEY=your_43_char_encoding_aes_key

# ---- 方案二：智能机器人 URL回调 ----
# 以上 CorpID/AgentId/Secret 全部留空即可；
# WECOM_TOKEN / WECOM_ENCODING_AES_KEY 改为「智能机器人 API设置·URL回调」页面里的值

# ---- 方案三：智能机器人 长连接（内网）----
# 在机器人 API设置页选择「长连接」方式后，页面会给出 BotID 与长连接专用 Secret
# （注意：与 URL 回调的 Token/EncodingAESKey 不是同一套，且两种方式二选一，切换会互相失效）
# WECOM_WS_BOT_ID=your_bot_id
# WECOM_WS_SECRET=your_long_connection_secret
# WECOM_WS_URL=wss://openws.work.weixin.qq.com
# WECOM_WS_HEARTBEAT=30

# ===== 服务监听 =====
# WECOM_SERVER_HOST=0.0.0.0
# WECOM_SERVER_PORT=8080
# WECOM_CALLBACK_PATH=/wecom

# ===== Kubernetes =====
# 留空则使用 ~/.kube/config；部署在集群内自动使用 in-cluster 配置
# KUBECONFIG=C:/Users/you/.kube/config
# 日志下载地址前缀：nginx 已把 /wecom-k8s-bot-logs/ 映射到容器 /app/logs（宿主机项目 logs/）
# 配置后，超长日志回复会给出可点击的下载链接；留空则只回服务器本地路径
# LOG_DOWNLOAD_BASE_URL=https://test1.btxnetwork.com
# K8S_LOG_DEFAULT_LINES=60
# K8S_LOG_MAX_LINES=2000

# ===== 消息体限制 =====
# 企业微信智能机器人 markdown 主动回复上限 20480 字节（默认值即上限，一般无需改；若 AIBOT_REPLY_TYPE=text 需自行调小到 2048 以内）
# MAX_REPLY_BYTES=20480
# 超长日志（超过上限）自动保存到项目 logs/ 目录（docker-compose 已挂载）；配置 LOG_DOWNLOAD_BASE_URL 后可提供下载链接
