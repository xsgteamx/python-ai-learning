# day08 最终学习笔记

## 01-晨测(无)

---

## 02-回顾(无)

---

## 03-继承概述

> 课件对应：面向对象之三大特性 > 继承

### ★ 课件核心重点
- 子类不能继承父类的私有属性和私有方法,因为存在名称改写,但是可以通过改写后的名称直接访问父类的私有成员,不过,这种做法违背了封装原则,不建议使用。
- 在类名后括号内指定要继承的父类。
- 可使用 类名.__mro__ 访问类的继承链来查看方法解析顺序。
- 子类可以新增自己特有的方法,也可以重写父类的方法。
- 调用方法时先在子类中查找,若不存在则从左到右依次查找父类中是否包含方法。
- 方法解析顺序(mro—Method Resolution Order)。

---

## 04-单继承

> 课件对应：面向对象之三大特性 > 继承 > 单继承

### ★ 课件核心重点
- 在类名后括号内指定要继承的父类。

---

## 05-多继承

> 课件对应：面向对象之三大特性 > 继承 > 多继承

### ★ 课件核心重点
- 调用方法时先在子类中查找,若不存在则从左到右依次查找父类中是否包含方法。

---

## 06-使用super()复用父类方法

> 课件对应：面向对象之三大特性 > 继承 > 复用父类方法

### ★ 课件核心重点
- super().方法名()。
- 父类名.方法名()。

---

## 07-MRO方法解析顺序

> 课件对应：面向对象之三大特性 > 继承 > 方法解析顺序

### ★ 课件核心重点
- 方法解析顺序(mro—Method Resolution Order)。
- 可使用 类名.__mro__ 访问类的继承链来查看方法解析顺序。
- super() 不是简单的 “调用父类”,而是基于方法解析顺序(MRO) 调用 “当前类在 MRO 链中的下一个类”。
- 具体使用哪个mro,取决于实例对象所属的类型。
- super() 的正确用法有两种:。

---

## 08-方法的重写

> 课件对应：面向对象之三大特性 > 继承 > 方法重写

### ★ 课件核心重点
- 在子类中定义与父类方法重名的方法,调用时会调用子类中重写的方法。
- 本节课件主题：方法重写。

---

## 09-抽象类

### ✦ 视频新增内容（课件未讲）
- 抽象类用于约束子类必须提供统一的方法入口，从代码层面规范方法名称和参数形式。
- Python 可通过继承 `ABC`，并使用 `@abstractmethod` 标记抽象方法；抽象方法可以只保留声明，方法体使用 `pass`。
- 子类若没有实现全部抽象方法，就不能实例化。
- 抽象类为多态提供统一接口：调用方依赖共同的方法名，各子类负责不同的具体实现。

---

## 10-多态

> 课件对应：面向对象之三大特性 > 多态

### ★ 课件核心重点
- 同一事物在不同场景下呈现不同状态。

---

## 11-综合案例：愤怒的小鸟

> 课件对应：面向对象案例：愤怒的小鸟

### ★ 课件核心重点
- name:用于标识每只小鸟的名称,方便区分不同个体。
- fly():描述小鸟飞行的基本动作,是小鸟在游戏中的常见行为,所有子类都可以重写该方法来展示不同的飞行特点。
- use_skill():用于触发小鸟的特殊技能,展示小鸟使用技能的情况,子类可以根据自身技能特点进行相应实现。
- 作为通用的飞行方法,提供了小鸟飞行的基本描述,子类可以根据自身特点进行个性化实现,以体现不同小鸟的飞行风格。
- 模拟小鸟发出叫声的行为,为游戏增加生动性,子类可以重写该方法来展示不同小鸟的叫声特点。
- 用于触发小鸟的特殊技能,通过输出技能描述,让玩家了解小鸟使用技能的情况,不同子类可以根据自身技能进行不同的实现。

---

## 12-错误和异常

> 课件对应：错误和异常 > 异常介绍

### ★ 课件核心重点
- 所以,只有在运行或测试程序时,才会真正知道该程序能不能正常运行。
- Python有两种错误很容易辨认:语法错误和异常。
- 程序解析时遇到的错误。
- 例如以下程序,因缺少 : 而出现语法错误。
- Python 程序的语法是正确的,在运行它的时候,也有可能发生错误。
- 运行期检测到的错误被称为异常。

---

## 13-捕获异常

> 课件对应：错误和异常 > 异常处理

### ★ 课件核心重点
- 无论是否发生异常都会执行的代码,通常用于执行一些必须要进行的清理操作,例如关闭文件、释放资源(如网络连接、数据库连接、锁等),即使在执行 try 块中的代码时出现了异常,也能保证这些操作得以完成。
- 对异常进行处理并不是将错误规避了,而是当程序运行的时候,出现错误的时候提供解决方案,不终止程序,可以让程序继续执行。
- 可以使用 try except 语句来捕获异常并处理。
- 如果没有发生异常,程序会忽略except中的代码,继续向下执行。
- 如果发生了异常,会忽略try中剩余代码,执行except中的代码。
- 在打印出来的异常信息中,冒号之前是异常类型,冒号之后是异常描述信息。

---

## 14-try...except

> 课件对应：错误和异常 > 异常处理 > try except

### ★ 课件核心重点
- 可以使用 try except 语句来捕获异常并处理。
- 如果没有发生异常,程序会忽略except中的代码,继续向下执行。
- 如果发生了异常,会忽略try中剩余代码,执行except中的代码。
- 本节课件主题：try except。

---

## 15-try...except...else

> 课件对应：错误和异常 > 异常处理 > else

### ★ 课件核心重点
- 可选地将else放在所有except之后。
- 如果try中代码没有发生异常,将执行 else 中的代码。
- 从执行效果上说,将代码放到else块和直接放到try块中是一样的。
- 将try正常执行完毕而没有引发任何异常后被执行的代码放到else中。
- 提供了一种清晰的逻辑区分,将正常情况的代码与异常处理代码分开,使代码更易于理解和维护,有助于代码的可读性和可维护性。
- 例如,你希望在try块中有些操作执行成功后,再执行其它代码,那就可以把代码放到else语句块中。

---

## 16-else和finally

> 课件对应：错误和异常 > 异常处理 > finally

### ★ 课件核心重点
- 无论是否发生异常都会执行的代码,通常用于执行一些必须要进行的清理操作,例如关闭文件、释放资源(如网络连接、数据库连接、锁等),即使在执行 try 块中的代码时出现了异常,也能保证这些操作得以完成。
- 如果从执行效果上说,大部分场景,将代码放到finally语句和放到try-except块外效果是一样的。
- finally 语句块是 try-except 结构的一部分,它确保了无论 try 块中是否发生异常,也无论 except 块是否被执行,其中的代码都会被执行。
- 可选地,放在最后。
- 直接放在try-except结构外面的代码只会在try-except结构正常执行完毕后才会执行,如果在try块中出现异常且没有被except块捕获,或者在except块中出现了新的异常导致程序终止,那么这部分代码将不会被执行。
