# day06 最终学习笔记

## 01-晨测和回顾(无)

---

## 02-变量作用域

> 课件对应：函数 > 变量的作用域

### ★ 课件核心重点
- 变量的作用域决定了哪一部分程序可以访问哪个变量,Python的作用域一共有4种,分别是:。
- L (Local) 局部作用域。
- E (Enclosing)嵌套作用域 闭包函数外的函数中。
- G (Global) 全局作用域。
- B (Built-in) 内建作用域。
- 以下案例演示各种作用域类型。

---

## 03-闭包和嵌套作用域

> 课件对应：函数 > 变量的作用域 > nonlocal关键字

### ★ 课件核心重点
- nonlocal 也用作内部作用域修改外部作用域的变量的场景,不过此时外部作用域不是全局作用域而是嵌套作用域。
- 本节课件主题：nonlocal关键字。

---

## 04-全局变量和局部变量

> 课件对应：函数 > 变量的作用域 > 全局变量和局部变量

### ★ 课件核心重点
- 定义在函数内部的变量拥有一个局部作用域,定义在函数外的拥有全局作用域。
- 局部变量只能在其被声明的函数内部访问,而全局变量可以在整个程序范围内访问。

---

## 05-递归

> 课件对应：函数 > 递归

### ★ 课件核心重点
- 递归一种是逻辑思想,将一个大工作分为逐渐减小的小工作,比如说一个和尚要搬50块石头,他想,只要先搬走49块,那剩下的一块就能搬完了,然后考虑那49块,只要先搬走48块,那剩下的一块就能搬完了......,递归是一种思想,只不过在程序中,就是依靠函数嵌套这个特性来实现了。
- 递归调用就是在函数体中又调用了函数本身.。
- 确定递归结束的条件。
- 确定它们之间的规律。
- 在定义递归函数的时候,主要确定两点。
- 递归案例 求一个整数n的阶乘!。

---

## 06-匿名函数

> 课件对应：函数 > 匿名函数

### ★ 课件核心重点
- lambda函数拥有自己的命名空间,且不能访问自己参数列表之外或全局命名空间里的参数。
- Python使用 lambda 来定义匿名函数,所谓匿名,指其不用 def 的标准形式定义函数。
- lambda 只是一个表达式,函数体比def简单很多。
- 可以将匿名函数与常用的内置参数搭配使用。
- map() 函数对序列中元素逐一处理。
- filter() 函数对序列中元素过滤。

---

## 07-匿名函数的使用

> 课件对应：函数 > 匿名函数

### ★ 课件核心重点
- lambda函数拥有自己的命名空间,且不能访问自己参数列表之外或全局命名空间里的参数。
- Python使用 lambda 来定义匿名函数,所谓匿名,指其不用 def 的标准形式定义函数。
- lambda 只是一个表达式,函数体比def简单很多。
- 可以将匿名函数与常用的内置参数搭配使用。
- map() 函数对序列中元素逐一处理。
- filter() 函数对序列中元素过滤。

---

## 08-函数的注释

> 课件对应：函数 > 函数的注释（了解）

### ★ 课件核心重点
- 如上,可以使用:对参数逐个进行注释,注释内容可以是任何形式,比如参数的类型、作用、取值范围等等,返回值使用->标注,所有的注释都会保存至函数的属性。
- 查看这些注释可以通过自定义函数的特殊属性__annotations__获取,结果会以字典的形式返回,另外,使用函数注释并不影响默认参数的使用。
- Python 3.x 引入了函数注释,以增强函数的注释功能。
- 本节课件主题：函数的注释（了解）。

---

## 09-文件操作概述

> 课件对应：文件操作 > 文件的基本概念

### ★ 课件核心重点
- 文件名通常包含文件的名称和扩展名,扩展名用于表示文件的类型(例如 .txt 表示文本文件,.jpg 表示图像文件)。
- 在计算机中,文件是存储在磁盘上的数据集合。
- 文件可以包含各种类型的数据,如文本、图像、音频、视频或程序代码。
- 文件系统通过文件名和文件路径来定位和管理文件。
- 文件路径可以是绝对路径(从文件系统的根目录开始)或相对路径(相对于当前工作目录)。
- 在编写程序的时候,数据是以二进制的形式存储在内存的,将数据写到磁盘文件的过程称之为持久化。

---

## 10-文件的读写

> 课件对应：文件操作 > 文件读写

### ★ 课件核心重点
- read([size]) 可以从文件中读取数据,size 表示要从文件中读取的数据的长度(编码模式b:单位是字节,编码模式t:单位是字符),如果没有传入 size 则读取文件中所有的数据。
- readlines([size]) 读取所有行并返回列表,若给定 size>0,返回总和大约为 size 字节的行, 实际读取值可能比 size 大。
- readline([size]) 可以从文件中读取整行数据,也可以通过 size 设置读取数据的长度。
- 本节课件主题：文件读写。
- 写数据。
- 读数据。

---

## 11-递归遍历目录

> 课件对应：文件操作 > 常用函数 > os.walk()递归遍历目录

### ★ 课件核心重点
- 本节课件主题：os.walk()递归遍历目录。
- 案例:递归遍历当前路径下所有目录和文件。

---

## 12-文件拷贝

> 课件对应：文件操作 > 案例：文件拷贝

### ★ 课件核心重点
- 优化:这种方式可以不用读取整个文件,减小内存压力。
- 本节课件主题：案例：文件拷贝。

---

## 13-面向对象概述

> 课件对应：面向对象之类和对象 > 面向过程和面向对象

### ★ 课件核心重点
- 面向过程编程(Procedural Programming)和面向对象编程(OOP)是两种不同的编程范式,它们在软件开发中都有广泛的应用。
- Python是一种混合型的语言,既支持面向过程的编程,也支持面向对象的编程。
- 面向过程的编程是一种以过程为中心的编程方式,主要关注解决问题的步骤,并将这些步骤写成函数或方法。
- 面向对象的编程是一种以对象为中心的编程方式,主要关注在解决问题的过程中涉及哪些对象以及这些对象如何交互。
- 在面向过程编程的思维下,你会把整个做饭的过程拆分成一系列的步骤。
- 上面就是一个典型的面向过程的程序,我们把整个做饭的过程分解成了一个个函数,每个函数完成一个特定的任务,然后按照顺序依次调用这些函数,就可以完成做晚餐的任务啦。

---

## 14-类的定义

> 课件对应：面向对象之类和对象 > 定义类

### ★ 课件核心重点
- 类名一般使用大驼峰命名法。
- 本节课件主题：定义类。

---

## 15-类的操作之成员访问

> 课件对应：面向对象之类和对象 > 类的操作 > 成员引用

### ★ 课件核心重点
- 语法：`类名.成员名`。
- 本节课件主题：成员引用。

---

## 16-类的操作之实例化

> 课件对应：面向对象之类和对象 > 类的操作 > 实例化

### ★ 课件核心重点
- 语法：`变量名 = 类名()`。

---

## 17-类的__init__()

> 课件对应：面向对象之类和对象 > __init()__方法

### ★ 课件核心重点
- __init__() 方法的主要作用是在创建类的对象时,对对象的属性进行初始化。
- 如果类中没有定义 __init__() 方法,Python 会使用默认的构造函数,该构造函数不执行任何操作。
- __init__() 方法只能返回 None,不能返回其他值。
- 如果尝试返回其他值,会引发 TypeError 异常。
- __init__() 是一个特殊的方法,也被称作构造函数。
- self:这是一个约定俗成的参数名,它代表类的实例对象本身。
