# day05 最终学习笔记

## 01-晨测和回顾(无)

---

## 02-集合概述

> 课件对应：容器数据类型 > 集合Set

### ★ 课件核心重点
- set.union(x):添加元素后返回一个新的集合,x可以为列表、元组、字符串、字典等可迭代对象。
- set.remove(x):从集合中移除x,x不存在则报错。
- set.discard(x):从集合中移除x,x不存在也不报错。
- set.pop():随机取出集合中的一个元素,如果集合为空则报错。
- set.clear():清空集合。
- set.difference(x1,...):求set1和x1的差集,返回一个新的集合。

---

## 03-集合常用操作

> 课件对应：容器数据类型 > 集合Set

### ★ 课件核心重点
- set.union(x):添加元素后返回一个新的集合,x可以为列表、元组、字符串、字典等可迭代对象。
- set.remove(x):从集合中移除x,x不存在则报错。
- set.discard(x):从集合中移除x,x不存在也不报错。
- set.pop():随机取出集合中的一个元素,如果集合为空则报错。
- set.clear():清空集合。
- set.difference(x1,...):求set1和x1的差集,返回一个新的集合。

---

## 04-集合常用函数

> 课件对应：容器数据类型 > 集合Set > 常用函数

### ★ 课件核心重点
- set.union(x):添加元素后返回一个新的集合,x可以为列表、元组、字符串、字典等可迭代对象。
- set.remove(x):从集合中移除x,x不存在则报错。
- set.discard(x):从集合中移除x,x不存在也不报错。
- set.pop():随机取出集合中的一个元素,如果集合为空则报错。
- set.clear():清空集合。
- set.difference(x1,...):求set1和x1的差集,返回一个新的集合。

---

## 05-字典概述

> 课件对应：容器数据类型 > 字典Dictionary

### ★ 课件核心重点
- dict.popitem():取出字典中的最后插入的键值对,字典为空则报错。
- dict.clear():清空字典。
- dict.get(key[,default]):获取字典中key对应value,可设置默认值。
- dict.setdefault(key[,default]):获取字典中key对应value,可设置默认值。若key不存在于字典中,将会添加key并将value设为默认值。
- dict.keys():获取字典所有的key,返回一个视图对象。字典改变,视图也会跟着变化。
- dict.values():获取字典所有的value,返回一个视图对象。

---

## 06-字典常用操作

> 课件对应：容器数据类型 > 字典Dictionary

### ★ 课件核心重点
- dict.popitem():取出字典中的最后插入的键值对,字典为空则报错。
- dict.clear():清空字典。
- dict.get(key[,default]):获取字典中key对应value,可设置默认值。
- dict.setdefault(key[,default]):获取字典中key对应value,可设置默认值。若key不存在于字典中,将会添加key并将value设为默认值。
- dict.keys():获取字典所有的key,返回一个视图对象。字典改变,视图也会跟着变化。
- dict.values():获取字典所有的value,返回一个视图对象。

---

## 07-字典常用函数

> 课件对应：容器数据类型 > 字典Dictionary > 常用函数

### ★ 课件核心重点
- dict.popitem():取出字典中的最后插入的键值对,字典为空则报错。
- dict.clear():清空字典。
- dict.get(key[,default]):获取字典中key对应value,可设置默认值。
- dict.setdefault(key[,default]):获取字典中key对应value,可设置默认值。若key不存在于字典中,将会添加key并将value设为默认值。
- dict.keys():获取字典所有的key,返回一个视图对象。字典改变,视图也会跟着变化。
- dict.values():获取字典所有的value,返回一个视图对象。

---

## 08-函数概述

> 课件对应：函数 > 函数的概念

### ★ 课件核心重点
- 函数是带名字的代码块,用于完成具体的任务,可重复使用。
- Python中的函数必须先定义后使用,Python提供了许多内建函数,比如print()。
- 当需要在程序中多次执行同一项任务时,无须反复编写完成该任务的代码,只需要调用执行该任务的函数,让Python运行其中的代码即可。
- 通过使用函数,程序编写、阅读、测试和修复起来都更加容易。
- 也可以自己创建函数,这被叫做用户自定义函数。
- 那么这个时候就可以通过定义函数来解决我们的问题。

---

## 09-函数的抽取和调用

> 课件对应：函数 > 函数的抽取以及调用

### ★ 课件核心重点
- 函数必须先定义再调用。
- 我们分析,可以将打印2x3的*这个功能封装为一个单独的函数。
- 函数定义好之后,通过函数名()对函数进行调用。
- 函数在定义的时候只是告诉解释器我定义了一个这样的函数,可以完成某些功能,但是这个时候函数还没有执行,需要调用函数后,才会执行。
- 在上面打印两次2x3的*的案例中,我们的代码出现了冗余。
- 本节课件主题：函数的抽取以及调用。

---

## 10-函数的参数

> 课件对应：函数 > 函数的参数

### ★ 课件核心重点
- 注意:这里我们虽然封装了函数,但是printStar1和printStar2这两个函数中的大部分代码还是一样的,存在冗余。
- 调用函数时,Python必须将函数调用中的每个实参都关联到函数定义中的一个形参。
- 定义函数时,可给每个形参指定默认值。
- 在调用函数时,给形参提供了实参则使用指定的实参值,否则使用形参的默认值。
- 因此,给形参指定默认值后,可在函数调用中省略相应的实参。
- 使用默认值可简化函数调用,还可清楚地指出函数的典型用法。

---

## 11-函数内存结构

> 课件对应：函数 > 函数的参数 > 函数的参数传递 > 引用的概念

### ★ 课件核心重点
- 注意:如果变量已经被定义,当给一个变量赋值的时候,本质上是修改了数据的引用,变量不再对之前的数据引用,变量改为对新赋值的数据引用,变量的名字类似于便签纸贴在数据上。
- 在 Python 中,变量和数据是分开存储的,数据保存在内存中的一个位置,变量中保存着数据在内存中的地址,变量中记录数据的地址,就叫做引用。
- 使用id()函数可以查看变量中保存数据所在的内存地址。
- 本节课件主题：引用的概念。

---

## 12-函数的参数传递

> 课件对应：函数 > 函数的参数 > 函数的参数传递

### ★ 课件核心重点
- Python 中一切都是对象,严格意义我们不能说值传递还是引用传递,我们应该说传不可变对象和传可变对象。
- 以上代码中,10是数字类型," helloworld " 是 String 类型,而变量 a 是没有类型,她仅仅是一个对象的引用(一个指针),可以是指向数字类型对象,也可以是指向 String 类型对象。
- 注意:如果变量已经被定义,当给一个变量赋值的时候,本质上是修改了数据的引用,变量不再对之前的数据引用,变量改为对新赋值的数据引用,变量的名字类似于便签纸贴在数据上。
- 使用id()函数可以查看变量中保存数据所在的内存地址。
- 类似c++的值传递,如整数、字符串、元组。
- 如fun(a),传递的只是a的值,没有影响a对象本身。

---

## 13-函数的参数传输可变对象和不可变对象

> 课件对应：函数 > 函数的参数 > 函数的参数传递 > 可变(mutable)与不可变(immutable)类型对象

### ★ 课件核心重点
- 在Python常见的类型中,数字类型、string、tuple是不可更改的对象,而list、set、dict等则是可以修改的对象。
- 变量赋值 a=500 后再赋值 a=1000,这里实际是新生成一个 int 值对象 1000,再让 a 指向它,而500被丢弃,不是改变a的值,相当于新生成了a。
- 变量赋值 la=[1,2,3,4] 后再赋值 la[2]=5 则是将 list la 的第三个元素值更改,本身la没有动,只是其内部的一部分值被修改了。
- 本节课件主题：可变(mutable)与不可变(immutable)类型对象。

---

## 14-函数的参数形式（1）

> 课件对应：函数 > 函数的参数 > 函数可使用的参数形式

### ★ 课件核心重点
- 调用函数时,Python必须将函数调用中的每个实参都关联到函数定义中的一个形参。
- 定义函数时,可给每个形参指定默认值。
- 在调用函数时,给形参提供了实参则使用指定的实参值,否则使用形参的默认值。
- 因此,给形参指定默认值后,可在函数调用中省略相应的实参。
- 使用默认值可简化函数调用,还可清楚地指出函数的典型用法。
- 注意:非默认参数必须放在默认参数之前。

---

## 15-函数的参数形式（2）

> 课件对应：函数 > 函数的参数 > 函数可使用的参数形式

### ★ 课件核心重点
- 调用函数时,Python必须将函数调用中的每个实参都关联到函数定义中的一个形参。
- 定义函数时,可给每个形参指定默认值。
- 在调用函数时,给形参提供了实参则使用指定的实参值,否则使用形参的默认值。
- 因此,给形参指定默认值后,可在函数调用中省略相应的实参。
- 使用默认值可简化函数调用,还可清楚地指出函数的典型用法。
- 注意:非默认参数必须放在默认参数之前。

---

## 16-浅拷贝和深拷贝

> 课件对应：Python高级语法 > 浅拷贝与深拷贝

### ★ 课件核心重点
- 直接赋值:对象的引用(别名),不产生拷贝。
- 浅拷贝:拷贝父对象,不会拷贝对象的内部的子对象。
- 拷贝后只有第一层是独立的。
- 深拷贝:完全拷贝了父对象及其子对象。
- 可以看到拷贝后新的列表地址改变了,但列表中各个元素还是同一地址。
- 可以看到拷贝后,新的列表地址与列表中各个可变类型元素的地址都发生了改变,不可变类型元素拷贝后地址不变。

---

## 17-函数的返回值

> 课件对应：函数 > 函数的定义 > 返回值

### ★ 课件核心重点
- 有些函数在执行的时候,需要有返回值给调用者,通过return关键字进行返回,返回到函数调用的位置。

---

## 18-函数的嵌套调用

> 课件对应：函数 > 函数嵌套调用

### ★ 课件核心重点
- 在一个函数中调用另一个函数,当内层函数执行完之后才会继续执行外层函数。
- 本节课件主题：函数嵌套调用。
