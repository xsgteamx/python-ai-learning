# day03 最终学习笔记

## 01-晨测和回顾(无)

---

## 02-输入函数input()

> 课件对应：基础知识 > 输入与输出 > 输入

### ★ 课件核心重点
- 如果接收用户在键盘上输入一些字符,Python提供了一个input()函数,可以让用户输入字符串,并存放到一个字符串变量里。
- 语法:字符串变量 = input(“提示信息”)。

---

## 03-输出函数print()

> 课件对应：基础知识 > 输入与输出 > 输出

### ★ 课件核心重点
- %d:十进制整数。
- %f:浮点数,%.nf可指定显示小数点后n位。
- %s:字符串。
- %o:八进制整数。
- %x:十六进制整数。
- %e:科学计数法。

---

## 04-格式化输出之占位符

> 课件对应：基础知识 > 输入与输出 > 输出 > 格式化输出 > 字符串中使用 % 占位

### ★ 课件核心重点
- %d:十进制整数。
- %f:浮点数,%.nf可指定显示小数点后n位。
- %s:字符串。
- %o:八进制整数。
- %x:十六进制整数。
- %e:科学计数法。

---

## 05-格式化输出之format

> 课件对应：基础知识 > 输入与输出 > 输出 > 格式化输出 > 字符串.format()

### ★ 课件核心重点
- 方式1:不设置指定位置,按默认顺序。
- 方式2:设置指定位置,不能和方式1混合使用。
- 方式3:设置参数。
- 本节课件主题：字符串.format()。

---

## 06-格式化输出之format处理浮点

> 课件对应：基础知识 > 输入与输出 > 输出 > 格式化输出 > 数字格式化：

### ★ 课件核心重点
- *:以 * 填充空白,不写则默认以空格填充。
- : 后可以添加多个参数对数字格式化:。
- ^:可选 < 、 ^ 、 > ,分别是左对齐、居中、右对齐。
- 20:数字宽度为20,数字长度不足20则进行填充。
- :可选 , 和 _ ,每3位进行分隔。
- .2f:小数点后保留2位。

---

## 07-格式化输出之f字符串

> 课件对应：基础知识 > 输入与输出 > 输出 > 格式化输出 > f-字符串

### ★ 课件核心重点
- 字符串前加上一个 f ,字符串中的{}内写入变量名。
- {}内变量名后可以加上 = ,打印时会在变量值前加上 变量名=。
- {}外再套一层{},即{{}},会转义。
- 本节课件主题：f-字符串。

---

## 08-算术运算符和赋值运算符

> 课件对应：基础知识 > 运算符 > 算数运算符；基础知识 > 运算符 > 赋值运算符

### ★ 课件核心重点
- :=:海象运算符,在表达式中同时进行赋值和返回赋值的值。Python3.8 版本新增:num1 = 20 print((num2 := 3**2) > num1) print(num2)。
- +:加:a + b。
- -:减、或取负:a - b、-a。
- *:乘:a * b。
- /:除:a / b。
- //:整除,除后向下取整:a // b。
- %:模,返回除法的余数:a % b。

---

## 09-比较运算符

> 课件对应：基础知识 > 运算符 > 比较运算符

### ★ 课件核心重点
- ==:相等,比较两者的值:a == b。
- !=:不相等:a != b。
- >:大于:a > b。
- <:小于:a < b。
- 注意:除了不同数据类型的数据不能比较大小。

---

## 10-逻辑运算符

> 课件对应：基础知识 > 运算符 > 逻辑运算符

### ★ 课件核心重点
- and:与,x and y,若x为False返回x的值,否则返回y的值。
- or:或,x or y,若x为True返回x的值,否则返回y的值。
- not:非,not x,若x为True返回False,若x为False返回True。

---

## 11-成员运算符和身份运算符

> 课件对应：基础知识 > 运算符 > 成员运算符；基础知识 > 运算符 > 身份运算符

### ★ 课件核心重点
- in:在指定的序列中找到值返回 True,否则返回 False:a in ['a', 'b', 'c']。
- is:判断两个标识符是不是引用自相同对象:a is b,类似id(a) == id(b)。如果引用的是同一个对象则返回True,否则返回False。

---

## 12-Python的编码规范

> 课件对应：基础知识 > Python编码规范

### ★ 课件核心重点
- 在 Python 中,代码块的结束不像其他一些编程语言(如 C、Java 等)使用大括号 {} 来明确界定,而是通过缩进来表示。
- 在大多数编辑器中,都可设置一个视觉标志,通常是一条竖线,让你知道不能越过的界线在什么地方。
- 你应该使用空行来组织程序文件,但也不能滥用。
- 为确保所有人编写的代码的结构都大致一致, Python 程序员都遵循一些格式设置约定。
- PEP8(Python Enhancement Proposal ,PEP)是最古老的PEP之一,它向 Python 程序员提供了代码格式设置指南。
- https://python.org/dev/peps/pep-0008/。

---

## 13-单分支

> 课件对应：流程控制语句 > 分支 > 单分支

### ★ 课件核心重点
- 其中"判断条件"成立时(非零),则执行后面的语句,而执行内容可以多行,以缩进来区分表示同一范围,缩进取消后,就不在分支范围了。
- if语句的判断条件可以用条件表达式来表示其关系,后面的:必须加。
- Python程序语言指定任何非0和非空(null)值为true,0 或者 null为false。
- 如果条件不成立,不执行语句块内容。
- 商品价格50,若余额小于50则提示“余额不足,请充值”,最后打印“欢迎下次光临”。

---

## 14-双分支

> 课件对应：流程控制语句 > 分支 > 双分支

### ★ 课件核心重点
- 先进行条件判断,如果条件判断成立就执行语句块1, 条件不成立就执行语句块2。
- 余额随机,商品价格50。
- 若余额小于50则提示“余额不足,请充值”。
- 否则提示消费成功。
- 最后打印“欢迎下次光临”。

---

## 15-多分支

> 课件对应：流程控制语句 > 分支 > 多分支

### ★ 课件核心重点
- 使用多分支语句的时候,需要注意下面几点:。
- if语句可以有若干个 elif 语句,它们必须在else语句之前。
- 一旦其中一个分支语句检测为 true,其他的elif以及else语句都将不再执行。
- 如果条件判断1成立,那么执行语句块1的内容;如果条件判断2成立,那么执行语句块2的内容;如果条件判断3成立,那么执行语句块3的内容;如果条件判断1,2,3都不成立,那么执行语句块4的内容。
- 判断处于人生哪个阶段。
- 如果年龄小于2岁,就打印一条消息,指出这个人是婴儿。

---

## 16-嵌套分支

> 课件对应：流程控制语句 > 分支 > 嵌套分支

### ★ 课件核心重点
- 在一个if语句中,又包含另外一个if语句,这就是if语句的嵌套。
- 判断输入法的状态:。
- 如果是大写状态,打印“大写状态”。
- 如果不是大写状态,判断输入法语言是“简体中文-微软拼音”还是“英语-美式键盘”。
- 如果是“简体中文-微软拼音”,判断是中文模式还是英文模式,并打印。
- 如果是“英语-美式键盘”,打印“英语-美式键盘”。

---

## 17-match...case和三目运算符

> 课件对应：流程控制语句 > 分支 > match case语句；流程控制语句 > 分支 > 三目运算符

### ★ 课件核心重点
- | 是专门用于模式匹配的操作符,它能把多个常量或者模式组合起来,实现 “或” 逻辑。
- 以上代码可以通过三目运算符改写。
- Python3.10新增了match case的条件判断方式,match后的对象会依次与case后的内容匹配,匹配成功则执行相应语句,否则跳过。
- 其中_可以匹配一切。
- 给定月份,求该月有多少天。
- 使用 if 来获取两个数中较大的一个。
- 本节课件主题：match case语句。

---

## 18-while循环案例1

> 课件对应：流程控制语句 > 循环 > while循环

### ★ 课件核心重点
- 先判断条件是否成立,如果条件成立就执行循环体一次;然后再判断条件是否成立,如果成立,继续执行循环体,直到循环条件不成立的时候,才会结束循环,执行循环下面的其他语句。
- 如果条件表达式一直成立,那称之为无限循环,也叫死循环。
- 此时else中代码,写在else中和写在循环外效果一样。
- else一般和 break一起使用,循环通过break终止后,else中的代码不会执行。
- 判断条件可以是任何表达式,任何非零、或非空(null)的值均为true。
- 执行语句可以是单个语句或语句块。

---

## 19-while...else

> 课件对应：流程控制语句 > 循环 > while循环 > while else语句

### ★ 课件核心重点
- 此时else中代码,写在else中和写在循环外效果一样。
- else一般和 break一起使用,循环通过break终止后,else中的代码不会执行。
- 本节课件主题：while else语句。

---

## 20-for循环

> 课件对应：流程控制语句 > 循环 > for循环

### ★ 课件核心重点
- 作用:函数可以生成数列,它返回一个可迭代对象。
- start: 生成序列的起始值--包含 默认0。
- step:生成序列的步长 默认为1。
- 指定生成到stop(不包含stop)的数列,默认从0开始。
- 指定生成数列的范围,从start到stop(不包含stop),可设定步长,默认步长为1,步长可正可负。
- for是关键字。

---

## 21-for循环案例

> 课件对应：流程控制语句 > 循环 > for循环

### ★ 课件核心重点
- 作用:函数可以生成数列,它返回一个可迭代对象。
- start: 生成序列的起始值--包含 默认0。
- step:生成序列的步长 默认为1。
- 指定生成到stop(不包含stop)的数列,默认从0开始。
- 指定生成数列的范围,从start到stop(不包含stop),可设定步长,默认步长为1,步长可正可负。
- for是关键字。
