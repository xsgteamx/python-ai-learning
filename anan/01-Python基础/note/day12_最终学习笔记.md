# day12 最终学习笔记

## 01-晨测和回顾(无)

---

## 02-正则表达式概述

> 课件对应：正则表达式 > 什么是正则表达式

### ★ 课件核心重点
- 正则表达式(regular expression,常简写为regex、regexp或re),是一种用于匹配和操作文本的强大工具,它是由一系列字符和特殊字符组成的模式,用于描述要匹配的文本模式。
- 正则表达式可以在文本中查找、替换、提取和验证特定的模式。
- 本节课件主题：什么是正则表达式。

---

## 03-re模块常用方法

> 课件对应：正则表达式 > re模块

### ★ 课件核心重点
- re模块中提供了一些方法用于查找或处理字符串。
- 返回通过使用 repl 替换在 string 最左边非重叠出现的 pattern 而获得的字符串。
- 扫描整个 string 查找正则表达式 pattern 产生匹配的第一个位置,并返回相应的 Match。
- 如果字符串中没有与模式匹配的位置则返回 None。
- 如果 string 开头的零个或多个字符与正则表达式 pattern 匹配,则返回相应的 Match。
- 返回 pattern 在 string 中的所有非重叠匹配,以字符串列表或字符串元组列表的形式。

---

## 04-正则表达式的语法

> 课件对应：正则表达式 > 表示字符；正则表达式 > 表示数量；正则表达式 > 表示边界；正则表达式 > 匹配分组

### ★ 课件核心重点
- \w:匹配包括下划线的任何单词字符。等价于 [A-Za-z0-9_],注意Unicode正则表达式会匹配中文字符。
- [xyz]:匹配所包含的任意一个字符。如 [abc] 可以匹配“plain”中的“a”。特殊字符仅有反斜线 \ 保持特殊含义,用于转义字符。其它特殊字符如星号、加号、各种括号等均作为普通字符。脱字符 ^ 如果出现在首位则表示负值字符集合;如果出现在字符串中间就仅作为普通字符。连字符 - 如果出现在字符串中间表示字符范围描述;如果出现在首位(或末尾)则仅作为普通字符。右方括号应转义出现,也可以作为首位字符出现。
- *:匹配前面的子表达式零次或多次。等价于 {0,}。
- \num:向后引用一个子字符串,该子字符串与正则表达式的第num个用括号围起来的子表达式匹配。其中num从1开始。例如:(.)\1 匹配两个连续的相同字符。
- .:匹配除 \r,\n 之外的任何单个字符。要匹配包括 \r,\n 在内的任何字符,请使用像 (.|\r|\n) 的模式。
- \d:匹配一个数字字符。等价于 [0-9]。
- \W:匹配任何非单词字符。等价于 [^A-Za-z0-9_]。

---

## 05-测试正则表达式

> 课件对应：正则表达式 > 案例

### ★ 课件核心重点
- 匹配电话号码。
- 匹配邮箱。
- 匹配0-255之间的数字。
- 从标签中获取网址。
- 替换文本中的所有数字为对应的词。

---

## 06-客户管理系统之客户类设计

> 课件对应：综合案例：客户信息管理系统 > 代码实现 > 客户类

### ★ 课件核心重点
- 课件对应主题：客户类。

---

## 07-客户管理系统之客户管理类

> 课件对应：综合案例：客户信息管理系统 > 代码实现 > 客户管理系统类

### ★ 课件核心重点
- 本节课件主题：客户管理系统类。

---

## 08-客户管理系统之增删改查功能分析

> 课件对应：综合案例：客户信息管理系统 > 需求说明

### ★ 课件核心重点
- 否则提示id必须为纯数字,并再次输入。
- 输入纯数字则正常添加。
- 第三次输入时有额外提醒。
- 三次输入失败后终止添加客户。
- id若已存在则终止添加。
- 输入字母或汉字则正常添加。

---

## 09-客户管理系统之编写客户信息类

> 课件对应：综合案例：客户信息管理系统 > 代码实现 > 客户类

### ★ 课件核心重点
- 本节课件主题：客户类。

---

## 10-客户管理系统之添加功能（1）

> 课件对应：综合案例：客户信息管理系统 > 需求说明 > 添加客户；综合案例：客户信息管理系统 > 代码实现 > 客户管理系统类

### ★ 课件核心重点
- 否则提示id必须为纯数字,并再次输入。
- 输入纯数字则正常添加。
- 第三次输入时有额外提醒。
- 三次输入失败后终止添加客户。
- id若已存在则终止添加。
- 输入字母或汉字则正常添加。
- 否则会有提示,并跳过添加年龄。

---

## 11-客户管理系统之添加功能（2）

> 课件对应：综合案例：客户信息管理系统 > 需求说明 > 添加客户；综合案例：客户信息管理系统 > 代码实现 > 客户管理系统类

### ★ 课件核心重点
- 否则提示id必须为纯数字,并再次输入。
- 输入纯数字则正常添加。
- 第三次输入时有额外提醒。
- 三次输入失败后终止添加客户。
- id若已存在则终止添加。
- 输入字母或汉字则正常添加。
- 否则会有提示,并跳过添加年龄。

---

## 12-客户管理系统之添加功能（3）

> 课件对应：综合案例：客户信息管理系统 > 需求说明 > 添加客户；综合案例：客户信息管理系统 > 代码实现 > 客户管理系统类

### ★ 课件核心重点
- 否则提示id必须为纯数字,并再次输入。
- 输入纯数字则正常添加。
- 第三次输入时有额外提醒。
- 三次输入失败后终止添加客户。
- id若已存在则终止添加。
- 输入字母或汉字则正常添加。
- 否则会有提示,并跳过添加年龄。

---

## 13-客户管理系统之删除功能

> 课件对应：综合案例：客户信息管理系统 > 需求说明 > 删除客户；综合案例：客户信息管理系统 > 代码实现 > 客户管理系统类

### ★ 课件核心重点
- 输入要删除的客户的id,不存在则终止,存在则删除。
- 本节课件主题：删除客户。
- 本节课件主题：客户管理系统类。

---

## 14-客户管理系统之修改功能

> 课件对应：综合案例：客户信息管理系统 > 需求说明 > 修改客户；综合案例：客户信息管理系统 > 代码实现 > 客户管理系统类

### ★ 课件核心重点
- 输入要修改的客户的id,只能修改年龄、电话、邮箱。
- 未设置的属性保持历史数据不变。
- 本节课件主题：修改客户。
- 本节课件主题：客户管理系统类。

---

## 15-客户管理系统之查询功能

> 课件对应：综合案例：客户信息管理系统 > 需求说明 > 查询客户；综合案例：客户信息管理系统 > 代码实现 > 客户管理系统类

### ★ 课件核心重点
- 可以按id或姓名查询,之前未设置的年龄、电话、邮箱显示 None。
- 本节课件主题：查询客户。
- 本节课件主题：客户管理系统类。

---

## 16-客户管理系统之功能测试

> 课件对应：综合案例：客户信息管理系统 > 代码实现

### ★ 课件核心重点
- 客户类。
- 客户管理系统类。
