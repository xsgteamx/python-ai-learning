# day09 最终学习笔记

## 01-晨测(无)

---

## 02-回顾(无)

---

## 03-使用raise抛异常

> 课件对应：错误和异常 > 抛出异常 > raise

### ★ 课件核心重点
- 当你想要在代码中明确表示发生了错误或异常情况时,可以使用 raise 来抛出异常。
- 这可以帮助你在满足某些条件时停止程序的正常执行,并将控制权转移到异常处理部分。

---

## 04-断言

> 课件对应：错误和异常 > 抛出异常 > assert断言

### ★ 课件核心重点
- assert用于判断一个表达式,在表达式条件为False的时候触发异常,常用于调试程序。
- 本节课件主题：assert断言。

---

## 05-自定义异常

> 课件对应：错误和异常 > 自定义异常

### ★ 课件核心重点
- 通过直接或者间接继承Exception类来创建自己的异常。

---

## 06-异常的传递

> 课件对应：错误和异常 > 异常的传递

### ★ 课件核心重点
- 当存在 try 嵌套或函数嵌套时,若内层出现了异常且在内层无法处理,会将异常一层一层向外传递,直到异常被处理或程序报错。

---

## 07-with关键字概述

> 课件对应：错误和异常 > with关键字

### ★ 课件核心重点
- Python中的with语句用于异常处理,封装了try except finally编码范式,提供了一种简洁的方式来确保资源的正确获取和释放,同时处理可能发生的异常,提高了易用性。
- 使用 with 关键字系统会自动调用 f.close() 方法, with 的作用等效于 try finally 语句。
- __enter__ 方法的返回值可以被存储在 variable 中(如果有),以供 with 代码块中使用。
- variable:是可选的,用于存储expression的__enter__方法的返回值。
- 当执行with语句时,会调用expression对象的__enter__方法。
- 然后执行 with 语句内部的代码块。

---

## 08-使用with操作文件读写

> 课件对应：错误和异常 > with关键字 > 案例：打开一个文件并向其中写入内容，验证出现异常后文件是否正常关闭。 > 使用 with

### ★ 课件核心重点
- 本节课件主题：使用 with。

---

## 09-模块和包

> 课件对应：模块与包 > 模块概述；模块与包 > 包概述

### ★ 课件核心重点
- 例如,模块名A.B表示名为A的包中名为B的子模块。
- 包与文件夹相似,不过该文件夹下必须有一个__init__.py文件。
- Python中一个以.py结尾的源文件即为一个模块(Module)。
- 通常情况下,我们把能够实现某一特定功能的代码放置在一个文件中作为一个模块。
- 使用模块提高了代码的可维护性,也提高了代码的复用性。
- 即编写好一个模块后,只要是实现该功能的程序,都可以导入这个模块实现。
- 另外,使用模块也可以避免名称冲突,相同名字的函数或变量可以分别存在与不同的模块中。

---

## 10-全局导入

> 课件对应：模块与包 > 导入模块 > 全部导入import

### ★ 课件核心重点
- 导入模块的所有成员,通过模块名.成员名的方式访问。
- 即使多次使用 import导入同一模块,模块也只会被导入一次。
- 在同一目录下创建一个main.py文件,在其中导入 my_add.py 模块并使用。
- 也可以在导入模块时给模块起别名。
- 语法：`import 模块名 [as 别名]`。
- 本节课件主题：全部导入import。

---

## 11-局部导入（1）

> 课件对应：模块与包 > 导入模块 > 局部导入 from import

### ★ 课件核心重点
- 只能使用其导入的成员,未导入的成员不能使用。
- 指定导入模块的部分成员,直接通过成员名的方式访问。
- 如果多个模块中存在重名成员,后一次导入会覆盖前一次导入。
- 只能使用导入的成员。
- 重名变量,后一次导入会覆盖前一次导入。
- 语法：`from 模块名 import 成员名1[as 别名], 成员名2[as 别名],...`。

---

## 12-局部导入（2）

> 课件对应：模块与包 > 导入模块 > 局部导入 from import

### ★ 课件核心重点
- 只能使用其导入的成员,未导入的成员不能使用。
- 指定导入模块的部分成员,直接通过成员名的方式访问。
- 如果多个模块中存在重名成员,后一次导入会覆盖前一次导入。
- 只能使用导入的成员。
- 重名变量,后一次导入会覆盖前一次导入。
- 语法：`from 模块名 import 成员名1[as 别名], 成员名2[as 别名],...`。

---

## 13-模块搜索顺序

> 课件对应：模块与包 > 导入模块 > 模块搜索顺序

### ★ 课件核心重点
- 当导入一个模块时,会按照以下顺序进行查找:。
- 可以使用以下方式查看模块搜索顺序:。
- 也可以通过 sys.path.append(路径) 向 sys.path 中临时添加路径。
- 当前目录。
- PYTHONPATH环境变量中的目录。
- 包含标准 Python 模块以及这些模块所依赖的任何 extension module 的目录。

---

## 14-特殊变量__all__

> 课件对应：模块与包 > 导入模块 > __all__

### ★ 课件核心重点
- 使用from import *导入模块时,可以在被导入的模块中使用 __all__设置哪些内容可以被导入。
- __all__ 的设置只针对使用 from import * 导入模块时有效。
- 在my_add.py 中向__all__添加部分元素:。
- 没在 __all__ 中的变量在使用时会报错:。
- 在main.py中使用 from my_add import *导入模块中的内容。
- 而使用import my_add全局导入模块后可以正常使用所有元素:。

---

## 15-内置变量__name__

> 课件对应：模块与包 > 导入模块 > __name__

### ★ 课件核心重点
- 在 Python 中,__name__ 是一个特殊的内置变量。
- 当一个Python文件被直接运行时,该文件的__name__属性值为"__main__"。
- 当一个Python文件作为模块被导入时,__name__属性会被设置为该模块的名称(即文件名,不包含 .py 后缀)。
- 为了避免模块被导入时测试代码被执行,我们可以在被导入模块中添加对 __name__ 属性的检查:。
- 有时我们会在模块中写一些测试代码,当模块被其他文件导入时这些测试代码会被执行。
- 在 my_add.py 中写一些测试代码:。

---

## 16-dir()

> 课件对应：模块与包 > dir()

### ★ 课件核心重点
- dir() 是一个内置函数,主要用于列出对象的属性和方法,或者列出当前作用域中定义的名称,并以一个字符串列表的形式返回。
- 当你将一个模块作为 dir() 的参数时,它会返回该模块中定义的名称列表,包括函数、类、变量等。
- 当你将一个对象作为 dir() 的参数时,它会返回该对象的属性和方法列表。
- 当你不传递任何参数调用 dir() 时,它会列出当前作用域中定义的名称,包括变量、函数、类等。

---

## 17-包概述

> 课件对应：模块与包 > 包概述

### ★ 课件核心重点
- 例如,模块名A.B表示名为A的包中名为B的子模块。
- 包与文件夹相似,不过该文件夹下必须有一个__init__.py文件。
- 语法：`sound/ 最高层级的包`。
- 语法：`__init__.py 初始化 sound 包`。
- 语法：`formats/ 用于文件格式转换的子包`。
- 语法：`__init__.py`。

---

## 18-创建包并使用全局导入

> 课件对应：模块与包 > 创建包；模块与包 > 导入包 > 全局导入import

### ★ 课件核心重点
- 如果最后一项是包,那么必须在被导入包的__init__.py文件中,指定导入包中的哪些模块。
- 使用 import 时,除最后一项外都必须是包。
- 最后一项可以是模块或包,但不能是类、函数或变量。
- 导入包中模块的所有成员。
- 这是python导包的一个优化机制,避免导入过多模块。
- 并且在__init__.py中指定导入模块的时候,建议使用相对路径。
- 如果是绝对路径,那么他会把导入的模块circle作为顶级模块,直接从sys.path中是找circle模块,而不是找graphic.circle。

---

## 19-包的局部导入

> 课件对应：模块与包 > 导入包 > 局部导入包下的模块 from import；模块与包 > 导入包 > 局部导入包下模块的成员 from import；模块与包 > 导入包 > 局部导入 from import * 从包中导入模块

### ★ 课件核心重点
- 当我们使用 from import * 时,Python并不会查找并导入包的所有子模块,因为这将花费很长的时间,并且可能会产生我们不想要的副作用。
- 从包中导入模块。
- 从包中模块导入功能。
- 唯一的解决办法是提供包的显式索引。
- 如果包的 __init__.py 中定义了 __all__,运行 from import * 时,它就是被导入的模块名列表。
- 在 main.py 中使用 from import * 导入模块:。
- 语法：`from 包名import模块名 [as 别名]`。

---

## 20-pip命令的使用

> 课件对应：模块与包 > 引入第三方库 > pip命令方式

### ★ 课件核心重点
- pip 默认的源是 Python Package Index(PyPI),其地址为 https://pypi.org/simple/,如果下载比较慢,还可以指定其它的源。
- pip是Python包管理工具,该工具提供了对 Python 包的查找、下载、安装、卸载的功能。
- 通过命令行的方式安装,是将第三方包安装在本地python下,例如:D:\dev\software\Python3.12.8\Lib\site-packages。
- 语法：`pip list`。
- 语法：`pip install 包名`。
- 语法：`pip install -i http://mirrors.aliyun.com/pypi/simple/ 包名`。

---

## 21-在PyCharm中下载第三方库

> 课件对应：模块与包 > 引入第三方库 > Pycharm中引入

### ★ 课件核心重点
- 通过Pycharm安装,我们选择的解释器类型每个项目独立的,所以是将第三方包安装在当前项目环境下,例如:D:\dev\workspace\python-2025\.venv\Lib\site-packages。
- 本节课件主题：Pycharm中引入。
- 点击右下角的解释器设置。
- 点击+号。
- 搜索要添加的包。

---

## 22-打包并安装自己的包

> 课件对应：模块与包 > 打包自己的库并安装

### ★ 课件核心重点
- 语法：`pip install setuptools`。
- 语法：`python setup.py sdist`。
- 语法：`pip install path_to_your_package/dist/your_package_name-0.1.tar.gz`。
- 如果不安装setuptools库,后续打包时可能会遇到报错 ModuleNotFoundError: No module named 'distutils',所以可以提前安装 setuptools 库。
- 在命令提示符中执行如下命令:。
- 本节课件主题：打包自己的库并安装。
