# day02 最终学习笔记

## 01-日考题讲解和回顾(无)

---

## 02-PyCharm中创建项目的方式

> 课件对应：快速入门 > 安装PyCharm > PyCharm设置

### ★ 课件核心重点
- 注意:不同的pycharm版本,看到的界面会略有不同。
- generate new表示为新项目基于你所选择虚拟环境中的python作为Base python生成一个虚拟环境,生成后放在其项目文件夹下的venv中,使用venv作为解释器。
- Select existing表示选择已安装的虚拟环境作为自己的编译器,并不会在项目文件夹下产生项目本身的venv,所选择的虚拟环境中的所有已安装的第三方包都可以使用,而不用重新安装。
- Ctrl + /:行注释(可选中多行)。
- Ctrl + Alt + L:代码格式化。
- Ctrl + C:复制当前行或选定的代码块到剪贴板。

---

## 03-变量的创建和赋值

> 课件对应：基础知识 > 变量 > 变量的创建

### ★ 课件核心重点
- 每个变量在使用前都必须赋值,变量赋值以后该变量才会被创建。
- 变量创建方式:变量名 = 变量值。
- Python中的变量不需要声明。
- 等号(=)用来给变量赋值。
- 等号(=)运算符左边是一个变量名,等号(=)运算符右边是存储在变量中的值。
- 多个变量的创建:。

---

## 04-标识符的命名规则

> 课件对应：基础知识 > 变量 > 标识符命名规则 > 命名规则

### ★ 课件核心重点
- 只能包含字母、数字和下划线,且不能以数字开头。
- 区分大小写,即Name和name是两个不同的标识符。
- 不要和关键字重复。

---

## 05-标识符的命名方法

> 课件对应：基础知识 > 变量 > 标识符命名规则 > 标识符命名方法

### ★ 课件核心重点
- 注意:Python源文件不遵循命名规范不影响程序的执行,但不建议。
- 常见的命名方法有三种:。
- 大驼峰命名法(upper camel case):每个单词首字母大写,例如UpperCamelCase。
- 小驼峰命名法(lower camel case):第一个单词首字母小写, 之后每个单词首字母大写,例如lowerCamelCase。
- 蛇形命名法(snake case):单词间用下划线连接,例如snake_case。
- 应既简短又具有描述性。

---

## 06-变量值互换和常量

> 课件对应：基础知识 > 变量 > 变量的修改；基础知识 > 变量 > 常量

### ★ 课件核心重点
- 一般约定使用全大写变量名来表示常量。
- 在程序中可随时修改变量的值,而Python将始终记录变量的最新值。
- Python还支持方便的对变量相互替换。
- 在程序中定义后就不再修改的值为常量,Python中没有内置的常量类型。
- 本节课件主题：变量的修改。

---

## 07-进制

> 课件对应：基础知识 > 进制以及转换 > 进制

### ★ 课件核心重点
- 0:0:0。
- 1:1:1。
- 10:2:2。
- 11:3:3。
- 100:4:4。
- 101:5:5。

---

## 08-Python中操作进制

> 课件对应：基础知识 > 进制以及转换 > 进制

### ★ 课件核心重点
- 0:0:0。
- 1:1:1。
- 10:2:2。
- 11:3:3。
- 100:4:4。
- 101:5:5。

---

## 09-进制的转换

> 课件对应：基础知识 > 进制以及转换

### ★ 课件核心重点
- 0:0:0。
- 1:1:1。
- 10:2:2。
- 11:3:3。
- 100:4:4。
- 101:5:5。

---

## 10-数据类型

> 课件对应：基础知识 > 数据类型

### ★ 课件核心重点
- type() 和 isinstance() 的区别在于 type() 不会认为子类是一种父类类型,isinstance() 会认为子类是一种父类类型。
- \:在行尾作为续行符。
- \\:反斜杠符号。
- \':单引号。
- \":双引号。
- \b:退格。

---

## 11-整型和布尔的关系

> 课件对应：基础知识 > 数据类型 > int整型；基础知识 > 数据类型 > bool布尔型

### ★ 课件核心重点
- 这种表示法适用于整数和浮点数,但只有Python 3.6及以上版本支持。
- type() 和 isinstance() 的区别在于 type() 不会认为子类是一种父类类型,isinstance() 会认为子类是一种父类类型。
- 上述提到的[-5, 256]范围是 CPython 的默认实现。
- 布尔型变量只有 True 和 False,用于真假的判断。
- is 运算符用于比较两个对象的身份(即它们是否是同一个对象,是否在内存中占据相同的位置),而不是比较它们的值。
- Python可以处理任意大小的整数,包括负整数。
- 书写很大的数时,可使用下划线将其中的数字分组,使其更清晰易读。

---

## 12-type()和isinstance()

> 课件对应：基础知识 > 数据类型 > int整型 > type 与 isinstance 类型判断

### ★ 课件核心重点
- type() 和 isinstance() 的区别在于 type() 不会认为子类是一种父类类型,isinstance() 会认为子类是一种父类类型。
- 本节课件主题：type 与 isinstance 类型判断。

---

## 13-小整数池

> 课件对应：基础知识 > 数据类型 > int整型 > 小整数池

### ★ 课件核心重点
- Python将 [-5, 256] 的整数维护在小整数对象池中。
- 这些整数提前创建好且不会被垃圾回收,避免了为整数频繁申请和销毁内存空间。
- 不管在程序的什么位置,使用的位于这个范围内的整数都是同一个对象。

---

## 14-浮点类型

> 课件对应：基础知识 > 数据类型 > float浮点型

### ★ 课件核心重点
- 要注意在使用浮点数进行计算时可能会存在微小误差,可以通过导入decimal解决。
- 也可以使用科学计数法表示浮点数。
- Python将所有带小数点的数称为浮点数。
- 本节课件主题：float浮点型。

---

## 15-布尔类型

> 课件对应：基础知识 > 数据类型 > bool布尔型

### ★ 课件核心重点
- 布尔型变量只有 True 和 False,用于真假的判断。
- is 运算符用于比较两个对象的身份(即它们是否是同一个对象,是否在内存中占据相同的位置),而不是比较它们的值。
- Python3中,bool 是 int 的子类,True 和 False 可以和数字相加。
- 在Python中,能够解释为假的值不只有False,还有:。
- 所有的空容器(空列表、空元组、空字典、空集合、空字符串)。
- 本节课件主题：bool布尔型。

---

## 16-字符串类型

> 课件对应：基础知识 > 数据类型 > String字符串初识

### ★ 课件核心重点
- 也可以使用三个引号表示多行字符串。
- 每个(字符串),不夹杂空格或者特殊符号,默认开启intern机制,共享内存,靠引用计数决定是否销毁。
- 相同的字符串默认只保留一份,当创建一个字符串,它会先检查内存里有没有这个字符串,如果有就不再创建新的了。
- \:在行尾作为续行符。
- \\:反斜杠符号。
- \':单引号。

---

## 17-字符串中的转义字符

> 课件对应：基础知识 > 数据类型 > String字符串初识

### ★ 课件核心重点
- 也可以使用三个引号表示多行字符串。
- 每个(字符串),不夹杂空格或者特殊符号,默认开启intern机制,共享内存,靠引用计数决定是否销毁。
- 相同的字符串默认只保留一份,当创建一个字符串,它会先检查内存里有没有这个字符串,如果有就不再创建新的了。
- \:在行尾作为续行符。
- \\:反斜杠符号。
- \':单引号。

---

## 18-自动类型转换

> 课件对应：基础知识 > 数据类型 > 数据类型转换 > 自动类型转换（隐式转换）

### ★ 课件核心重点
- 而整型和字符串相加会报错,此时Python无法进行隐式转换完成计算:。
- 特别的,两个整型进行除法运算结果也是浮点型:。
- 本节课件主题：自动类型转换（隐式转换）。

---

## 19-强制类型转换

> 课件对应：基础知识 > 数据类型 > 数据类型转换 > 强制类型转换（显式转换）

### ★ 课件核心重点
- int(x [,base]):将x转换为一个整数,x若为字符串可用base指定进制。
- float(x):将x转换为一个浮点数。
- str(x):将对象x转换为一个字符串。
- repr(x):将对象x转换为一个字符串,可以转义字符串中的特殊字符。
- bin(x):将一个整数转换为一个二进制字符串。
- ord(x):将一个字符转换为它的ASCII整数值。

---

## 20-编码和解码

> 课件对应：基础知识 > 数据类型 > 字符的编码和解码

### ★ 课件核心重点
- 本节课件主题：字符的编码和解码。
