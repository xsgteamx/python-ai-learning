# day04 最终学习笔记

## 01-晨测和回顾(无)

---

## 02-for...else和continue

> 课件对应：流程控制语句 > 循环 > continue

### ★ 课件核心重点
- 跳过当前循环块中的剩余语句,继续进行下一轮循环。
- 一般写在if判断中。

### ✦ 视频新增内容（课件未讲）
- `for ... else` 中的 `else` 只在循环正常结束时执行；若通过 `break` 提前退出循环，则跳过 `else`。

---

## 03-break和pass

> 课件对应：流程控制语句 > 循环 > break；流程控制语句 > 循环 > pass

### ★ 课件核心重点
- 如果for 或while循环通过break终止,循环对应的else 将不执行。
- pass是空语句,是为了保持程序结构的完整性。
- pass不做任何事情,一般用做占位语句。
- 这个时候我们就可以使用pass占位。
- 跳出当前for或while的循环体,一般写在if判断中。
- 例如:在一个循环中,如果循环体为空,语法会提示报错。

---

## 04-列表List概述

> 课件对应：容器数据类型 > 列表List

### ★ 课件核心重点
- list.insert(index,x):在指定位置插入x。
- list.append(x):在列表末尾追加x。
- list1.extend(list2):在列表1的末尾追加列表2的数据。
- del list[index]:删除指定位置的数据或切片。
- list.remove(x):删除第一次出现的x。
- list.pop([index]):删除指定位置的数据,默认为末尾数据。

---

## 05-列表的创建

> 课件对应：容器数据类型 > 列表List > 创建列表

### ★ 课件核心重点
- 本节课件主题：创建列表。

---

## 06-列表获取数据和切片操作

> 课件对应：容器数据类型 > 列表List > 访问列表

### ★ 课件核心重点
- 本节课件主题：访问列表。
- 通过索引获取列表中元素。
- 列表切片。

---

## 07-列表中添加数据、相加和相乘

> 课件对应：容器数据类型 > 列表List > 向列表中添加元素；容器数据类型 > 列表List > 列表相加；容器数据类型 > 列表List > 列表乘法

### ★ 课件核心重点
- 本节课件主题：向列表中添加元素。
- 本节课件主题：列表相加。

---

## 08-列表中数据的修改

> 课件对应：容器数据类型 > 列表List > 修改列表中元素

### ★ 课件核心重点
- 本节课件主题：修改列表中元素。
- 通过下标修改。
- 通过切片修改。

---

## 09-列表获取最大值、最小值、求和

> 课件对应：容器数据类型 > 列表List > 求列表中元素的最大值、最小值、加和

### ★ 课件核心重点
- 本节课件主题：求列表中元素的最大值、最小值、加和。

---

## 10-列表的遍历

> 课件对应：容器数据类型 > 列表List > 遍历列表

### ★ 课件核心重点
- 本节课件主题：遍历列表。
- 直接遍历列表元素。
- 通过下标遍历列表。
- 使用enumerate()同时获取列表的下标和元素。

---

## 11-列表推导式

> 课件对应：容器数据类型 > 列表List > 列表推导式

### ★ 课件核心重点
- 列表推导式是 Python 中一种简洁创建列表的方式,它将一个可迭代对象(如列表、元组、集合、字符串等)的元素通过某种运算或条件筛选后生成一个新的列表。
- 基础的列表推导式。
- 带条件的列表推导式。
- 使用现有列表的列表推导式。
- 包含多个循环的列表推导式。

---

## 12-列表常用函数

> 课件对应：容器数据类型 > 列表List > 常用函数

### ★ 课件核心重点
- list.append(x):在列表末尾追加x。
- list1.extend(list2):在列表1的末尾追加列表2的数据。
- list.clear():清空列表中元素。
- list1[start:end] = list2:修改列表切片的数据。
- sorted(list[,reverse=True]):返回排序后的新列表,可选降序。
- list.sort([reverse=True]):对列表就地排序,可选降序。

---

## 13-列表删除数据的问题

> 课件对应：容器数据类型 > 列表List > 删除列表指定位置元素或者切片

### ★ 课件核心重点
- 本节课件主题：删除列表指定位置元素或者切片。

### ✦ 视频新增内容（课件未讲）
- 遍历列表时直接删除元素会使后续元素的索引前移，可能导致相邻目标元素被跳过，出现漏删。
- 视频通过 Debug 断点逐步观察索引和列表内容的变化，用于定位“遍历过程中修改容器”造成的错位问题。

---

## 14-字符串常用功能

> 课件对应：容器数据类型 > 字符串String

### ★ 课件核心重点
- str.replace(old,new[,max]):把将字符串中的old替换成new,如果指定max,则替换不超过max次。
- str.split([x][,n]):按x分隔字符串,默认按任何空白字符串分隔并在结果中丢弃空字符串。可指定最大分隔次数。
- x.join(seq):以x作为分隔符,将序列中所有的字符串合并为一个新的字符串。
- str.strip([x]):截掉字符串两边的空格或指定字符。
- str.removeprefix():截掉字符串指定前缀。
- str.upper():将所有字符转为大写。

---

## 15-字符串常用函数

> 课件对应：容器数据类型 > 字符串String > 常用函数

### ★ 课件核心重点
- str.replace(old,new[,max]):把将字符串中的old替换成new,如果指定max,则替换不超过max次。
- str.split([x][,n]):按x分隔字符串,默认按任何空白字符串分隔并在结果中丢弃空字符串。可指定最大分隔次数。
- x.join(seq):以x作为分隔符,将序列中所有的字符串合并为一个新的字符串。
- str.strip([x]):截掉字符串两边的空格或指定字符。
- str.removeprefix():截掉字符串指定前缀。
- str.upper():将所有字符转为大写。

---

## 16-字符串其他函数

> 课件对应：容器数据类型 > 字符串String > 其他函数

### ★ 课件核心重点
- str.center(width[,x]):返回长度为width且居中的字符串,空白使用x填充,默认为空格。
- str.zfill(width):返回长度为width且右对齐的字符串,空白使用0填充。
- str.splitlines([keepends]):按行分隔字符串,返回每行字符串组成的列表,可选是否保留换行符。
- str.partition(x):使用x将字符串分隔为3部分,如果分隔后不足3部分或字符串中没有x则以空白填充。
- str.encode(encoding='UTF-8',errors='strict'):对字符串使用指定格式编码,并指定错误处理方案。
- str.expandtabs([tabsize]):将字符串中\t转化为空格,可指定每个\t空格数。

---

## 17-元组

> 课件对应：容器数据类型 > 元组Tuple

### ★ 课件核心重点
- 不能对元组中的元素进行修改操作。
- 元组是一个不可变的、有序的元素集合。
- 元组使用 () 定义,数据之间使用,分隔。
- 元组中每个元素都有对应的位置值,称为索引或下标,索引从起始从0开始向后逐个递增,并且从末尾从-1开始逐个向前递减。
- 元组中元素可以是不同的类型。
- 元组的使用方式与列表类似。
